import './h.js'
export const {
    allEvents, getLabel, reqFile, requestFile, delayedDispatch, wait, getEventNames, hasEvent, CURRENT_TARGET, AUTO_ABORT, TRUSTED, ONCE, PREVENT_DEFAULT, PASSIVE, CAPTURE, STOP_PROPAGATION, STOP_IMMEDIATE_PROPAGATION, addCustomEvent, on, off, until, getObjUrl, download, delegate, dispatch
} = globalThis[Symbol.for("[[HModule]]")]